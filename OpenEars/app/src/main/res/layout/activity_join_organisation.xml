<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".JoinOrganisationActivity"
    android:background="@color/colorBackground">
    <androidx.appcompat.widget.Toolbar
        android:id="@+id/joinorgbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        android:background="@color/colorAppBar"/>
    <TableLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:layout_marginLeft="10dp"
        android:layout_marginRight="10dp"
        app:layout_constraintTop_toBottomOf="@+id/joinorgbar">
        <EditText
            android:id="@+id/joinorgname"
            android:hint="@string/organisation_name"
            android:textSize="20sp"
            android:inputType="text"
            android:textAlignment="center"
            android:textColorHint="@color/colorText"
            android:textColor="@color/colorText"
            android:background="@color/colorBackground"/>
        <Button
            android:layout_marginTop="20dp"
            android:background="@color/colorButtons"
            android:textColor="@color/colorBackground"
            android:text="@string/preview_organisation"
            android:onClick="previewOrganisation"/>
        <Button
            android:layout_marginTop="20dp"
            android:background="@color/colorButtons"
            android:textColor="@color/colorBackground"
            android:text="@string/join_organisation"
            android:onClick="joinOrganisation"/>
    </TableLayout>
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginTop="250dp"
        android:layout_marginBottom="10dp"
        android:layout_marginLeft="10dp"
        android:layout_marginRight="10dp">
        <TableLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">
            <TextView
                android:id="@+id/jointitle"
                android:textColor="@color/colorText"
                android:textSize="40sp"
                android:textAlignment="center"/>
            <ImageView
                android:id="@+id/joinimage"
                android:layout_width="match_parent"
                android:scaleType="centerInside"/>
            <TextView
                android:id="@+id/joinsubtitle"
                android:textColor="@color/colorText"
                android:textSize="15sp"
                android:layout_marginTop="10dp"/>
        </TableLayout>
    </ScrollView>
</androidx.constraintlayout.widget.ConstraintLayout>